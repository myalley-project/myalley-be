package com.myalley.simpleReview_deleted.repository;

import com.myalley.simpleReview_deleted.domain.SimpleReviewDeleted;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SimpleReviewDeletedRepository extends JpaRepository<SimpleReviewDeleted, Long> {
}
