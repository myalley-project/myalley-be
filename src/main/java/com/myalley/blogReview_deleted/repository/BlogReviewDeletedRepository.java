package com.myalley.blogReview_deleted.repository;

import com.myalley.blogReview_deleted.domain.BlogReviewDeleted;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BlogReviewDeletedRepository extends JpaRepository<BlogReviewDeleted,Long> {
}
